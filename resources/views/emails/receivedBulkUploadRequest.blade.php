<div>
    <p>{{$data['name']}} {{translate('has requested and downloaded the Bulk Upload File.')}}</p>
    <p>{{translate('Information')}}:</p>
    <div>{{$data['company']}}</div>
    <div>{{$data['numOfSites']}}</div>
    <div>{{$data['message']}}</div>
    <p>{{translate("If you haven't heard back, contact them to ask, if they need help.")}}</p>
</div>